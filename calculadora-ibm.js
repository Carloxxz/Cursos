const bmiCalculadora = (peso, altura)=>{
    let bmi = peso / altura**2;
    return bmi;
}

bmiCalculadora(65, 1.8);

function bmiCalculator(weight, height){
    let bmi = weight/height**2;
    return bmi
}

bmiCalculator(65, 1.8);